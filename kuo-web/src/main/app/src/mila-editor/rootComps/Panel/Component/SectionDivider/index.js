import React from 'react';

import './index.less';

export default (props) => (
  <div className="section-divider labeled">
    <div className="section-divider-content">{props.content}</div>
  </div>
)
